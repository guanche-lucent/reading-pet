# 协作约定（给 AI 助手看的规则）

> 这个文件在本仓库每次会话自动生效。**与用户口头指令冲突时，以用户当次明确指令为准；但下面第 1 条不可绕过。**

## 1. 涉及"对外"的操作，必须先问再动 ⛔

以下动作**一律先说明计划、等用户明确同意后才能执行**，不允许"顺手做了"：

| 动作 | 必须先说明 |
|---|---|
| `git push`（任何分支、任何远程） | 仓库地址、分支名、将要上传的提交（hash + 一句话）、是否含敏感内容 |
| 修改远程配置（`git remote add/set-url/remove`） | 原地址 → 新地址、为什么改 |
| 删除/强制推送远程分支、`--force`、`git reset` 已推送的提交 | 为什么必须、影响谁 |
| 创建远程仓库、改仓库可见性 | 平台、账号、公开还是私有 |
| 发布/部署、上传文件到任何外部服务 | 目标地址、上传内容清单 |

**教训来源**：2026-09-16，助手按仓库既有 origin 直接执行 `git push`，推到 `github.com/hubooooooo/agent-blueprint` 被 403 拒绝。虽然没造成损坏，但**推之前没有向用户确认目标仓库**，这是错的。

## 2. 密钥与凭据

- 不打印密钥明文；输出只给掩码，且**示例一律用假值**（如 `sk-abcdef***Z`），不得用真钥匙的任何片段
- 不把密钥写进报告、文档、提交信息
- 提交前检查暂存区是否含密钥；`.env` 必须在 `.gitignore` 里
- 需要密钥的改动（填 `.env`）由用户自己执行；助手只在用户要求时核验（只读 + 掩码）

## 3. 花钱的操作

- 调用模型前说明：预计调用次数、token 量级、是否触及预算上限
- 单次超过约 10 万 token，或涉及新供应商/新密钥时，先确认
- 触达预算上限时停下来说明原因，**不自动续跑绕过上限**

## 4. 文件安全

- 删除、移动、覆盖文件前先说明；批量操作前给出清单
- 不修改引擎（`agent-blueprint`）源码，除非用户明确要求；改动走 git 提交，可回退
- 用户的项目内容不放进引擎目录（引擎只当工具用）

## 5. 收尾要说清

每次交付后汇报：**改了什么、文件在哪、怎么回退、还剩什么没做**。不确定的事标"待确认"，不编。
